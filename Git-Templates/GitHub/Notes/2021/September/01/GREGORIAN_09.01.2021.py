# Start of script
# A sample graphical calendar using the Gregorian calendar
# This script is highly incomplete, and needs work. It is currently just pseudocode
import random()
import math()
class MeadowsGregorianCalendar():
  year = int(2013)
  dayOfYear = int(1)
  return leapYear()
  if (leapYear == "true"):
    dayOfYearMax = int(366)
  elif (leapYear == "false"):
    dayOfYearMax = int(365)
  dayOfYearMin = int(1)
  def leapYear():
    leapYear = bool("true")
    if (year / 4 is not prime):
      leapYear == ("true")
      break
    elif (year /4 is prime):
      leapYear == ("false")
      break
  def JanuaryGraphics()
    _file_January from wd(January/)
    return file(January/*)
    break
  def FebruaryGraphics()
    _file_February from wd(February/)
    return file(February/*)
    break
  def MarchGraphics()
    _file_March from wd(March/)
    return file(March/*)
    break
  def AprilGraphics()
    _file_April from wd(April/)
    return file(April/*)
    break
  def MayGraphics()
    _file_May from wd(May/)
    return file(May/*)
    break
  def JuneGraphics()
    _file_June from wd(June/)
    return file(June/*)
    break
  def JulyGraphics()
    _file_July from wd(July/)
    return file(July/*)
    break
  def AugustGraphics()
    _file_August from wd(August/)
    return file(August/*)
    break
  def SeptemberGraphics()
    _file_September from wd(September/)
    return file(September/*)
    break
  def OctoberGraphics()
    _file_October from wd(October/)
    return file(October/*)
    break
  def NovemberGraphics()
    _file_November from wd(November/)
    return file(November/*)
    break
  def DecemberGraphics()
    _file_December from wd(December/)
    return file(December/*)
    break
  def GregJanGraph():
    for dayOfYear in range(1, 31):
      if (dayOfYear < 0 and dayOfYear > 32):
        monthJan = bool(true)
        print ("January")
        return JanuaryGraphics()
        break
  def GregFebGraph():
    if (leapYear == "true"):
    for dayOfYear in range(32, 71):
      # Factor for leap years
      if (dayOfYear < 31 and dayOfYear > 72):
        monthFeb = bool(true)
        print ("February")
        return FebruaryGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(32, 70):
      # Factor for leap years
      if (dayOfYear < 31 and dayOfYear > 71):
        monthFeb = bool(true)
        print ("February")
        return FebruaryGraphics()
        break
  def GregMarGraph():
    if (leapYear == "true"):
    for dayOfYear in range(72, 103):
      # Factor for leap years
      if (dayOfYear < 71 and dayOfYear > 104):
        monthMar = bool(true)
        print ("March")
        return MarchGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(71, 102):
      # Factor for leap years
      if (dayOfYear < 70 and dayOfYear > 103):
        monthMar = bool(true)
        print ("March")
        return MarchGraphics()
        break
  def GregAprGraph():
    if (leapYear == "true"):
    for dayOfYear in range(104, 134):
      # Factor for leap years
      if (dayOfYear < 103 and dayOfYear > 135):
        monthApr = bool(true)
        print ("April")
        return AprilGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(103, 133):
      # Factor for leap years
      if (dayOfYear < 102 and dayOfYear > 134):
        monthApr = bool(true)
        print ("April")
        return AprilGraphics()
        break
  def GregMayGraph():
    if (leapYear == "true"):
    for dayOfYear in range(135, 166):
      # Factor for leap years
      if (dayOfYear < 134 and dayOfYear > 167):
        monthMay = bool(true)
        print ("May")
        return MayGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(134, 165):
      # Factor for leap years
      if (dayOfYear < 133 and dayOfYear > 165):
        monthMay = bool(true)
        print ("May")
        return MayGraphics()
        break
  def GregJuneGraph():
    if (leapYear == "true"):
    for dayOfYear in range(167, 197):
      # Factor for leap years
      if (dayOfYear < 166 and dayOfYear > 198):
        monthJune = bool(true)
        print ("June")
        return JuneGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(166, 196):
      # Factor for leap years
      if (dayOfYear < 165 and dayOfYear > 197):
        monthJune = bool(true)
        print ("June")
        return JuneGraphics()
        break
  def GregJulyGraph():
    if (leapYear == "true"):
    for dayOfYear in range(198, 229):
      # Factor for leap years
      if (dayOfYear < 197 and dayOfYear > 230):
        monthJuly = bool(true)
        print ("July")
        return JulyGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(197, 228):
      # Factor for leap years
      if (dayOfYear < 196 and dayOfYear > 229):
        monthJuly = bool(true)
        print ("July")
        return JulyGraphics()
        break
  def GregAugustGraph():
    if (leapYear == "true"):
    for dayOfYear in range(230, 261):
      # Factor for leap years
      if (dayOfYear < 229 and dayOfYear > 262):
        monthAugust = bool(true)
        print ("August")
        return AugustGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(229, 260):
      # Factor for leap years
      if (dayOfYear < 228 and dayOfYear > 261):
        monthAugust = bool(true)
        print ("August")
        return AugustGraphics()
        break
  def GregSeptemberGraph():
    if (leapYear == "true"):
    for dayOfYear in range(262, 292):
      # Factor for leap years
      if (dayOfYear < 261 and dayOfYear > 293):
        monthSeptember = bool(true)
        print ("September")
        return SeptemberGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(261, 291):
      # Factor for leap years
      if (dayOfYear < 260 and dayOfYear > 292):
        monthSeptember = bool(true)
        print ("September")
        return SeptemberGraphics()
        break
  def GregOctoberGraph():
    if (leapYear == "true"):
    for dayOfYear in range(293, 324):
      # Factor for leap years
      if (dayOfYear < 292 and dayOfYear > 325):
        monthOctober = bool(true)
        print ("October")
        return OctoberGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(292, 323):
      # Factor for leap years
      if (dayOfYear < 291 and dayOfYear > 324):
        monthOctober = bool(true)
        print ("October")
        return OctoberGraphics()
        break
  def GregNovemberGraph():
    if (leapYear == "true"):
    for dayOfYear in range(325, 355):
      # Factor for leap years
      if (dayOfYear < 324 and dayOfYear > 356):
        monthNovember = bool(true)
        print ("November")
        return NovemberGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(324, 354):
      # Factor for leap years
      if (dayOfYear < 323 and dayOfYear > 355):
        monthNovember = bool(true)
        print ("November")
        return NovemberGraphics()
        break
  def GregDecemberGraph():
    if (leapYear == "true"):
    for dayOfYear in range(356, 387):
      # Factor for leap years
      if (dayOfYear < 355 and dayOfYear > 388):
        monthDecember = bool(true)
        print ("December")
        return DecemberGraphics()
        break
    elif (leapYear == "false"):
      for dayOfYear in range(355, 386):
      # Factor for leap years
      if (dayOfYear < 354 and dayOfYear > 387):
        monthDecember = bool(true)
        print ("December")
        return DecemberGraphics()
        break        
# There is a major calculation somewhere, I cannot fix it right now, and this source code REALLY Needs to be optimized

# File infe
# File type: Python source file (*.py)
# File version: 1 (Wednesday, 2021 September 1st at 4:16 pm)
# Line count (including blank lines and compiler line): 272
# End of script

