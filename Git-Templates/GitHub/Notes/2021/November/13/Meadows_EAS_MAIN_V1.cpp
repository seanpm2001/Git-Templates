// Start of script
#include <iostream>
#include <vector>
#include <stdexcept>
// I am not too experienced with C++ so some of this may not work
using namespace std;
class emergencyAlertSystemForMeadows {
	int main() {
		std::cout << "🚨 { Emergency Alert System } 🚨";
		return alerts();
		std::cout << "⚙️ Settings:\n🎶 Audio format\n📖 Subtitles\n🌐 Language\n🔘 On/off";
		if continue audioFormat() {
			std::cout << "🎶 Emergency Alert System advanced settings";
			std::cout << "Change preferred audio format (OGG, WAV, MP3) a very miniscule setting, for when it is needed";
			if continue {
				return preferenceAudioFormat();
				break;
			} else {
				goto 8;
				break;
		}
		if continue subtitles() {
			std::cout << "📖 Emergency Alert System subtitles settings";
			std::cout << "Enable/disable subtitles, customize subtitles";
			// Settings coming soon
			if continue {
				return subtitleSettings();
				break;
			} else {
				goto 8;
				break;
			}
		}
		if continue language() {
			char curLang = "EN-US"
			std::cout << "🌐 Emergency Alert System language settings";
			std::cout << "Current language is: " + curLang();
			std::cout << "Changing the language is currently not available";
			// Settings coming soon
			if continue {
				return languageSettings();
				break;
			} else {
				goto 8;
				break;
			}
		}
		if continue onOff() {
			std::cout << "🔘 Emergency Alert System";
			bool onOff << "Turn on Turn off";
			if bool == true {
				std::cout << "The Emergency Alert System has been turned off. You will no longer hear alerts from this program until you re-enable it";
				break;
			} else {
				std::cout << "The Emergency Alert System has been turned on. You will hear alerts from this program when emergencies occur.";
				break;
			}
		}
		break;
	}
	break;
	} // Connects to main (L:8)
	int alerts() {
		if alertCount < 0 {
			std::cout << "No recent emergency alerts.";
			break;
		} else {
			return alertList();
			break;
		}
	int alertList() {
			// Method coming soon
			std::cout << "Alerts are currently dysfunctional";
			break;
		}
	int soundTheAlarm() {
		if preferenceAudioF == "OGG" {
			// Coming soon
			break;
		} else if preferenceAudioF == "WAV" {
			// Coming soon
			break;
		} else if preferenceAudioF == "MP3" {
			// Coming soon
			break;
		}
	int preferenceAudioFormat() { // This is an advanced setting that is done as an extra feature. It doesn't change much
		std::cout << "Emergency Alert audio format preference:\nWhat format do you wish to hear alerts in?\nOGG\nWAV\nMP3\n";
		str preferenceAudioFor << "<< ";
		if std::preferenceAudioFor == "OGG" {
			preferenceAudioF == "OGG";
			std::cout << "Preference emergency alert audio set to OGG";
			break;
		} else if std::preferenceAudioFor == "WAV" {
			preferenceAudioF == "WAV";
			std::cout << "Preference emergency alert audio set to WAV";
			break;	
		} else if std::preferenceAudioFor == "MP3" {
			preferenceAudioF == "MP3";
			std::cout << "Preference emergency alert audio set to MP3";
			break;	
		} else {
			std::cout  "Unsupported file format: " + preferenceAudioFor + " reverting to default (OGG)";
			preferenceAudioF == "OGG";
			break;		
			}
		}
	}
	int subtitleSettings() {
		// Coming soon
		std::cout << "Subtitle settings are coming soon";
		break;	
	}
	int languageSettings() {
		// Coming soon
		std::cout << "Language settings are coming soon";
		break;
	}
	/** Features needed:
	01* The program to become functional
	02* A cropped version of the EAS tones
	03* Improved language support
	04* Support for incoming alerts
	05* A GUI version
	06* A better CLI version
	07* Improved documentation
	08* Ability to play test tones
	09* Full OGG support
	10* Full WAV support
	12* Full MP3 support
	12* Approval from the National Emergency Alert System
	13* Ability to send notifications
	14* More settings
	***/
}
/* File info
* File type: C++ source file (*.cpp, *.cxx)
* File version: 1 (2021, Saturday, November 13th at 2:07 pm)
* Line count (including blank lines and compiler line): 142
*/
// End of script

