// Start of script
#include <iostream>
using namespace std;
class deltabook_menu_main() { // This file is currently pseudocode and is likely not functional. It aims to show what the file is planned to do
/* This is the source code for the Deltabook menu. */
int dbook() {
  // This is the data pack for the Deltabook menu. It contains the editable data for the menu.
  string(dbook_chap1 == "The Beginning");
  string(dbook_chap2 == "A Cyber's world");
  string(dbook_chap3 == "--");
  string(dbook_chap4 == "--");
  string(dbook_chap5 == "--");
  string(dbook_chap6 == "--");
  string(dbook_chap7 == "--");
  string(dbook_title1 == "Chapter 1");
  string(dbook_title2 == "Chapter 2");
  string(dbook_title3 == "Chapter 3");
  string(dbook_title4 == "Chapter 4");
  string(dbook_title5 == "Chapter 5");
  string(dbook_title6 == "Chapter 6");
  string(dbook_title7 == "Chapter 7");
  break;
  goto 8;
  std::BinaryObject << chaframe == [bin/00000000]; // Incomplete and non-functional, it is the editable spritesheet data for the frame holding the chapter images
  std::BinaryObject << chap1Pic == [bin/00000001]; // Incomplete and non-functional, it is the editable spritesheet data for the chapter 1 image
  std::BinaryObject << chap2Pic == [bin/00000010]; // Incomplete and non-functional, it is the editable spritesheet data for the chapter 2 image
  std::BinaryObject << chap3Pic == [bin/00000011]; // Incomplete and non-functional, it is the editable spritesheet data for the chapter 3 image
  std::BinaryObject << chap4Pic == [bin/00000100]; // Incomplete and non-functional, it is the editable spritesheet data for the chapter 4 image
  std::BinaryObject << chap5Pic == [bin/00000101]; // Incomplete and non-functional, it is the editable spritesheet data for the chapter 5 image
  std::BinaryObject << chap6Pic == [bin/00000110]; // Incomplete and non-functional, it is the editable spritesheet data for the chapter 6 image
  std::BinaryObject << chap7Pic == [bin/00000111]; // Incomplete and non-functional, it is the editable spritesheet data for the chapter 7 image
  break;
} // End of deltabook data book method

int deltabookPreview() {
  // Deltabook preview: this method outputs a preview of the current version of the deltabook menu with its current data
  std::cout << string(dbook_title1) + string(dbook_chap1) + object(chaframe(object(chap1pic));
  std::cout << string(dbook_title2) + string(dbook_chap2) + object(chaframe(object(chap2pic));
  std::cout << string(dbook_title3) + string(dbook_chap3) + object(chaframe(object(chap3pic));
  std::cout << string(dbook_title4) + string(dbook_chap4) + object(chaframe(object(chap4pic));
  std::cout << string(dbook_title5) + string(dbook_chap5) + object(chaframe(object(chap5pic));
  std::cout << string(dbook_title6) + string(dbook_chap6) + object(chaframe(object(chap6pic));
  std::cout << string(dbook_title7) + string(dbook_chap7) + object(chaframe(object(chap7pic));
  std::cout << "Quit " + "Customize";
  break;
} // End of preview method

int dbook_edit() {
  // This is the editor for the deltabook data booklet. It currently cannot receive data and like the rest of the file, is not yet functional
  std::cout << "Editing Deltabook menu";
  std::cout << scanner.input("Would you like to change the image frame? (y/n) " + input(fileUpload));
  std::cout << scanner.input("Chapter 1 (NOT THE CHAPTER NAME) - Rename to: ");
  std::cout << scanner.input("Chapter 1 - Rename to: ");
  std::cout << scanner.input("Upload a new image for chapter 1" + input(fileUpload));
  std::cout << scanner.input("Chapter 2 (NOT THE CHAPTER NAME) - Rename to: ");
  std::cout << scanner.input("Chapter 2 - Rename to: ");
  std::cout << scanner.input("Upload a new image for chapter 2" + input(fileUpload));
  std::cout << scanner.input("Chapter 3 (NOT THE CHAPTER NAME) - Rename to: ");
  std::cout << scanner.input("Chapter 3 - Rename to: ");
  std::cout << scanner.input("Upload a new image for chapter 3" + input(fileUpload));
  std::cout << scanner.input("Chapter 4 (NOT THE CHAPTER NAME) - Rename to: ");
  std::cout << scanner.input("Chapter 4 - Rename to: ");
  std::cout << scanner.input("Upload a new image for chapter 4" + input(fileUpload));
  std::cout << scanner.input("Chapter 5 (NOT THE CHAPTER NAME) - Rename to: ");
  std::cout << scanner.input("Chapter 5 - Rename to: ");
  std::cout << scanner.input("Upload a new image for chapter 5" + input(fileUpload));
  std::cout << scanner.input("Chapter 6 (NOT THE CHAPTER NAME) - Rename to: ");
  std::cout << scanner.input("Chapter 6 - Rename to: ");
  std::cout << scanner.input("Upload a new image for chapter 6" + input(fileUpload));
  std::cout << scanner.input("Chapter 7 (NOT THE CHAPTER NAME) - Rename to: ");
  std::cout << scanner.input("Chapter 7 - Rename to: ");
  std::cout << scanner.input("Upload a new image for chapter 7" + input(fileUpload));
  std::cout << "Confirm changes: ";
  break;
} // End of menu editor

int main() {
  dbook();
  deltabookPreview();
  dbook_edit();
  break;
  return 0;
  exit;
} // End of main method

} // End of class
/* File info
* This is still very much a work in progress. I also decided to write this in a language I know less about (C++)
* to match what the engine the original games were made in (gamemaker)
* File type: C++ source file (*.cpp *.cxx)
* Line count (including blank lines and compiler line): 95
* File version: 1 (Sunday, 2021 October 10th at 4:08 pm)
*/
// End of script

