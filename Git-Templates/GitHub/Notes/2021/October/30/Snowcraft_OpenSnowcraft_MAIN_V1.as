// Start of script
package // Hello world program
{
    import flash.display.Sprite;
    import flash.text.TextField;
    
    public class actionscript extends Sprite
    {
        private var hello:TextField = new TextField();
        
        public function actionscript(){
            hello.text = "Hello, World!";
            addChild(hello);
        }
    }
}
public class Main
{
// Note: I do not know how to program in ActionScript, so this is just a sample pseudocode script for now
public var referenceStage:Stage = new Stage();
public function GameStart()
{
new MyCustomObject(stage);
break;
}
public function cpu()
{
	render:CPUChar { null } // No data available for CPU (The opposing team)
	// Not yet available
	break;
}
public function teamColorSelect()
{
	// Change the color of teams
	teamColorList = ["blue", "green", "red", "yellow", "purple", "orange", "brown", "black", "grey", "pink"];
	colorCHange.text = "Change your team color:\nCPU color: " + teamColorList() + "\nPlayer color: " + teamColorList());
	// Not yet available
	break;
}
public function levelList()
{
	// A list of levels (1-100)
	// level 0 is a level where you can throw snowballs at nothing
	// CPU count, CPU difficulty, CPU health (manual)
	// Not yet available
	break;
}
public function buildMode()
{
	// Build your own snow fort
	// Not yet available
	break;	
}
public function multiplayerMode()
{
	// Play against other players
	// Not yet available
	break;
}
public function levelSelect()
{
	lvlsel.text = "Select a level: " + levelList();
}
public function CPU-levels()
{ // Start of CPU-levels
	/*void function level1()
	{
		new object.cpu(3); // Summons 3 red CPUs
		object.cpu.difficulty(1); // Sets the difficulty to 1
		set object.cpu.health(3);
		break;
	}*/
	// I just thought of a better idea
	var set CPUcount = 3;
	var set CPUdifficulty = 1;
	var set CPUhealth = 3;
	for levelUp()
	{ // Start of LevelUp
		CPUcount == CPUCount + 2;
		CPUDifficulty == CPUDifficulty + 0.5;
		CPUHealth == CPUHealth + 0.25;
	} // End of levelUp
	if level == 100 {
		levelUp = bool(false); // The level should not go past 100
		/* Level 100 stats:
		* CPU count: 203
		* CPU difficulty: 51
		* CPU health: 28 (28 snowballs to knockout)
		*/
	} // End of CPU-levels
	// Not yet available
	break;
} // End of class


/* Feature ideas:
* Multiplayer mode
* Level selection
* Building snow forts
*/
/* File info
* File type: ActionScript 3.0 source code (*.as)
* File version: 1 (2021, Saturday, October 30th at 7:31 pm)
* Line count (including blank lines and compiler line): 107
*/
// End of script

