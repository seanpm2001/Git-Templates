// Start of script
# include <stdio.h>
// I am not too good at programming in C, so for now, this is mostly pseudocode.
int osDetect(void)
{
  printf("Running operating system detection test...");
  scanf("%///%");
  if (scanf == "///$C:/") {
    operatingSystemName = string("Windows"); // Go deeper to find the specific version of Windows
    break;
  } else if (scanf == "///home/"); {
    operatingSystemName = string("a UNIX-like operating system"); // Go deeper to find the specific UNIX-like system (MacOS v?, Linux, distro? v?, other, v?, ??)
    break;
  }
  break;
  // This is impractical, but is pseudocode
}
int main(void)
{
  // Title
  printf("About UTPM 2.0");
  // Body
  return osDetect();
  wait 1.5;
  printf("You are running UTPM 2.0 on " + operatingSystemName());
  printf("UTPM (UnTrustable Platform Module) is a reverse engineered tool for mitigating the effects of the Trusted Platform Module chipset that is being imposed on systems in the name of 'security' but ultimately, it turns your system into a jail and puts your computer in control of you, rather than you being in control of your computer.");
  printf("Join the fight against TPM and other DRM. [http://www.defectivebydesign.org/](http://www.defectivebydesign.org/)");
  break;
  exit;
}
// End of pseudocode

/* File info
* File type: C source file (*.c)
* File version: 1 (2021, Monday, October 18th at 2:29 pm)
* Line count (including blank lines and compiler line): 39
*/
// End of script

