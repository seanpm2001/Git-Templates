// Start of script
program LanguageFile(output);
begin
    Write('Merge Plane is closed source, and I do not know what language it is written in, so I chose the one programming language that is also a unit of distance (Pascal) as I want this project to have a language assigned to it, so I wrote this file in Pascal')
    {No ";" is required after the last statement of a block -
        adding one adds a "null statement" to the program, which is ignored by the compiler.}
end.

// Merge Plane is closed source, and I don't know what language it is written in, so I chose the one programming language that is also a unit of distance (Pascal) as I want this project to have a language assigned to it, so I wrote this file in Pascal

// File version: 1 (Monday, October 26th 2020 at 11:12 am)
// File type: Pascal script file (*.pas)
// Line count (including blank lines and compiler line): 15
// End of script
