<div id="page">
  <h2>{{projectlanguagefile}}</h2>

  {% paginate blog.articles by 20 %}

    {% for article in blog.articles  %}

    <div class="article">
      <div class="headline">
      <h3 class="title">
        <a href="{{article.url}}">{{ project-language-file-one }}</a>
      </h3>
      <h4 class="date">Posted on {{ article.created_at | date: "2022, Friday, October 7th @ 8:17 pm" }} by {{ "seanpm2001" }}.</h4>
      </div>

      <div class="article-body textile">
        {{ "I decided to make Liquid the main project language file for this project (Seanpm2001/Learn-Liquid) as Liquid is the language this project is dedicated to, because this project is about learning the Liquid programming language. It only makes sense to Liquid the official language for this project. It is getting its own project language file, starting here." | strip_html | truncate: 512 }}
      </div>

      {% if blog.comments_enabled? %}
        <p style="text-align: right"><a href="{{article.url}}#comments">{{ article.comments_count }} comments</a></p>
      {% endif %}
    </div>

    {% endfor %}

    <div id="pagination">
      {{ paginate | default_pagination }}
    </div>

  {% endpaginate %}

</div>
