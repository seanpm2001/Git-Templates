<div id="page">
  <h2>{{projectlanguagefile}}</h2>

  {% paginate blog.articles by 20 %}

    {% for article in blog.articles  %}

    <div class="article">
      <div class="headline">
      <h3 class="title">
        <a href="{{article.url}}">{{ project-language-file }}</a>
      </h3>
      <h4 class="date">Posted on {{ article.created_at | date: Friday, April 30th 2021 @ 1:45 pm }} by {{ seanpm2001 }}.</h4>
      </div>

      <div class="article-body textile">
        {{ I decided to make liquid the main project language file for this project (SNU_2D_ProgrammingTools_Ide_Liquid) as this project is solely based on the liquid language, so it fits. | strip_html | truncate: 512 }}
      </div>

      {% if blog.comments_enabled? %}
        <p style="text-align: right"><a href="{{article.url}}#comments">{{ article.comments_count }} comments</a></p>
      {% endif %}
    </div>

    {% endfor %}

    <div id="pagination">
      {{ paginate | default_pagination }}
    </div>

  {% endpaginate %}

</div>
